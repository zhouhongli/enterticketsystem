# Application
APP_NAME=企业售后工单系统
APP_ENV=local

# Data
TICKET_DATA_FILE=backend/data/store.json

# Session
SESSION_COOKIE_NAME=ticket_session
SESSION_COOKIE_SECURE=false
SESSION_TTL_HOURS=8

# First admin initialization.
# Fill these values only in a local .env file. Do not commit real secrets.
INITIAL_ADMIN_USERNAME=
INITIAL_ADMIN_EMAIL=
INITIAL_ADMIN_PASSWORD=
