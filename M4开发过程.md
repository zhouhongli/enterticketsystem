# M4 开发过程

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-05-30 |
| 当前阶段 | `M4` 内部处理闭环，先从 `M4-01` 内部工单列表、详情和单状态筛选开始 |
| 前置状态 | `M0`、`M1`、`M2`、`M3` 已完成 |
| 记录范围 | M4 阶段相关问答、决策、任务拆分、执行过程与验收结论 |

## 1. M4-01 启动记录

### 1.1 阶段确认

用户询问下一阶段是否为 `M04`。

核对结果：

- `docs/04-实施计划/01-MVP实施计划.md` 中正式编号为 `M4`，不是 `M04`。
- `M3开发过程.md` 已记录 `M3-01` 至 `M3-05` 全部完成，下一步为 `M4-01`。
- 本阶段只进入 `M4-01`：客服/管理员内部工单列表、详情和单状态筛选。

### 1.2 M4-01 范围

| 范围内 | 范围外 |
| --- | --- |
| `GET /api/v1/internal/tickets` | 分配或重新分配工单 |
| `GET /api/v1/internal/tickets/{ticket_id}` | 内部人员新增留言 |
| 客服、管理员查看全部工单 | 状态推进 |
| 内部列表按单一 `status` 筛选 | 关闭工单确认 |
| 内部详情返回公开留言和该工单操作记录 | 内部页面动态接线 |
| 内部响应不返回客户邮箱或敏感认证字段 | 搜索、分页、高级筛选、统计、附件、通知、SLA |

### 1.3 实现记录

本次未启动子智能体，由主智能体完成窄切片。

新增文件：

- `backend/app/services/internal_ticket_service.py`
- `backend/tests/api/test_internal_ticket_reads.py`

修改文件：

- `backend/app/api/dependencies.py`
- `backend/app/api/routes/internal.py`
- `backend/app/repositories/json_repository.py`
- `backend/app/schemas/tickets.py`

实现内容：

- 新增 `require_internal_user`，仅允许客服和管理员访问内部工单接口。
- 接通 `GET /api/v1/internal/tickets`，支持省略 `status` 返回全部，支持单一工单状态筛选。
- 拒绝非 `status` 查询参数和重复 `status` 参数。
- 接通 `GET /api/v1/internal/tickets/{ticket_id}`。
- 内部列表返回标题、客户简要信息、分类快照、状态、负责人简要信息和创建时间。
- 内部详情返回标题、描述、分类快照、客户简要信息、状态、负责人、时间、公开留言和该工单操作记录。
- 公开留言按 `sent_at` 正序，操作记录按 `occurred_at` 倒序。
- 响应不返回客户邮箱、密码哈希、会话字段、`customer_user_id`、`assignee_user_id` 或 `actor_user_id`。

未扩大范围：

- 未实现 `PATCH /internal/tickets/{id}/assignment`。
- 未实现 `POST /internal/tickets/{id}/messages`。
- 未实现 `PATCH /internal/tickets/{id}/status`。
- 未实现内部列表页和详情页动态接线。

### 1.4 验证记录

专项测试：

```text
.\.venv\Scripts\python.exe -m pytest tests/api/test_internal_ticket_reads.py
```

结果：

```text
5 passed
```

全量回归：

```text
.\.venv\Scripts\python.exe -m pytest
```

结果：

```text
54 passed
```

说明：

- 本次未启动本地开发或测试服务，未占用端口。
- 已复查 `8000-8010` 常用测试端口，结果为无监听端口。
- 未使用子智能体，无需关闭子智能体线程。

## 2. M4-01 阶段结论

`M4-01` 内部工单列表、详情和单状态筛选已完成。

已满足：

- 客服和管理员可查看全部工单列表。
- 内部列表可按单一工单状态筛选。
- 内部列表按创建时间倒序返回。
- 客服和管理员可查看任意工单详情。
- 内部详情返回公开留言和该工单操作记录。
- 公开留言按发送时间正序返回，操作记录按发生时间倒序返回。
- 未登录访问内部接口返回 `401`，客户访问内部接口返回 `403`。
- 内部响应不返回客户邮箱、密码哈希、会话字段或底层外键字段。

未扩大范围：

- 分配/重新分配仍属于 `M4-02`。
- 内部公开留言仍属于 `M4-03`。
- 状态推进和关闭限制仍属于 `M4-04`。
- 内部页面动态接线仍属于 `M4-05`。

下一步可进入 `M4-02`：管理员分配与重新分配服务，记录负责人变更。

## 3. M4-02 启动记录

### 3.1 用户请求

用户要求进入 `M4-02`。

执行口径：

- 严格限定在 `M4-02`：管理员分配与重新分配工单。
- 不提前实现内部公开留言、状态推进、关闭确认或内部页面动态接线。
- 不扩大搜索、分页、统计、通知、附件、SLA 等范围外能力。

### 3.2 M4-02 范围

| 范围内 | 范围外 |
| --- | --- |
| `PATCH /api/v1/internal/tickets/{ticket_id}/assignment` | 内部人员新增公开留言 |
| 仅管理员可分配或重新分配 | 工单状态推进 |
| 目标负责人必须是当前有效客服账号 | 关闭工单确认 |
| 未关闭工单允许首次分配或重新分配 | 内部页面动态接线 |
| 已关闭工单拒绝重新分配 | 客服处理权限落地到留言/状态接口 |
| 成功后写入负责人变更操作记录 | 搜索、分页、高级筛选、统计、附件、通知、SLA |
| 分配本身不自动改变工单状态 | 工单取消、重开、删除 |

### 3.3 实现记录

本次未启动子智能体，由主智能体完成窄切片。

新增文件：

- `backend/tests/api/test_internal_ticket_assignment.py`

修改文件：

- `backend/app/api/routes/internal.py`
- `backend/app/repositories/json_repository.py`
- `backend/app/schemas/tickets.py`
- `backend/app/services/internal_ticket_service.py`

实现内容：

- 新增 `InternalTicketAssignmentRequest`，请求体仅允许 `assignee_user_id`。
- 接通 `PATCH /api/v1/internal/tickets/{ticket_id}/assignment`。
- 接口使用管理员权限依赖，客服和客户均不可分配。
- 仓储层在同一 JSON 事务内校验工单、目标客服、关闭状态，更新负责人、更新时间并写入操作记录。
- 首次分配写入 `ticket_assigned`。
- 重新分配写入 `ticket_reassigned`。
- 操作记录保存 `assignee_user_id` 的变更前后值。
- 成功响应仅返回 `ticket_id`、负责人简要信息、当前状态和更新时间，不返回邮箱、底层外键、会话或认证敏感字段。
- 分配不自动改变工单状态。

未扩大范围：

- 未实现 `POST /internal/tickets/{id}/messages`。
- 未实现 `PATCH /internal/tickets/{id}/status`。
- 未实现内部列表页和详情页动态接线。

### 3.4 验证记录

专项测试：

```text
.\.venv\Scripts\python.exe -m pytest tests/api/test_internal_ticket_assignment.py
```

结果：

```text
5 passed
```

内部工单相关测试：

```text
.\.venv\Scripts\python.exe -m pytest tests/api/test_internal_ticket_reads.py tests/api/test_internal_ticket_assignment.py
```

结果：

```text
10 passed
```

全量回归：

```text
.\.venv\Scripts\python.exe -m pytest
```

结果：

```text
59 passed
```

说明：

- 本次未启动本地开发或测试服务，未占用端口。
- 已复查 `8000-8010` 常用测试端口，结果为无监听端口。
- 未使用子智能体，无需关闭子智能体线程。

## 4. M4-02 阶段结论

`M4-02` 管理员分配与重新分配服务已完成。

已满足：

- 管理员可将未关闭工单分配给有效客服。
- 管理员可将未关闭工单从原客服改派给另一有效客服。
- 分配和改派均不会自动改变工单状态。
- 首次分配和重新分配分别写入 `ticket_assigned`、`ticket_reassigned` 操作记录。
- 操作记录包含操作者、时间和负责人变更前后值。
- 非管理员角色不能分配工单。
- 目标用户不存在、不是客服、不是有效客服时拒绝分配且不改变原负责人。
- 已关闭工单拒绝重新分配。
- 非法请求体不会改变工单或写入操作记录。

未扩大范围：

- 内部公开留言仍属于 `M4-03`。
- 状态推进和关闭限制仍属于 `M4-04`。
- 内部页面动态接线仍属于 `M4-05`。

下一步可进入 `M4-03`：内部公开留言权限。

## 5. M4-03 启动记录

### 5.1 用户请求

用户要求进入 `M4-03`。

执行口径：

- 严格限定在 `M4-03`：内部公开留言权限。
- 不提前实现工单状态推进、关闭确认或内部页面动态接线。
- 不扩大搜索、分页、统计、通知、附件、SLA 等范围外能力。

### 5.2 M4-03 范围

| 范围内 | 范围外 |
| --- | --- |
| `POST /api/v1/internal/tickets/{ticket_id}/messages` | 工单状态推进 |
| 管理员可给任一未关闭工单留言 | 关闭工单确认 |
| 当前负责客服可给本人负责且未关闭工单留言 | 分配或重新分配逻辑调整 |
| 非负责客服不可给非本人负责工单留言 | 内部页面动态接线 |
| 已关闭工单禁止新增留言 | 搜索、分页、高级筛选、统计、附件、通知、SLA |
| 留言保存发送人快照并更新工单 `updated_at` | 留言编辑、删除 |
| 留言不自动改变工单状态 | 工单取消、重开、删除 |

### 5.3 实现记录

本次未启动子智能体，由主智能体完成窄切片。

新增文件：

- `backend/tests/api/test_internal_ticket_messages.py`

修改文件：

- `backend/app/api/routes/internal.py`
- `backend/app/repositories/json_repository.py`
- `backend/app/schemas/tickets.py`
- `backend/app/services/internal_ticket_service.py`

实现内容：

- 新增 `InternalTicketMessageCreateRequest`，请求体仅允许 `content`，长度 1 至 2000，自动去除首尾空白。
- 接通 `POST /api/v1/internal/tickets/{ticket_id}/messages`，成功返回 `201 Created`。
- 接口使用内部人员权限依赖，客户不可调用内部留言接口。
- 仓储层在同一 JSON 事务内校验工单、关闭状态和客服负责人权限。
- 管理员可给任一未关闭工单留言，包括未分配工单。
- 客服仅可给当前分配给自己的未关闭工单留言。
- 留言保存 `sender_user_id`、发送人角色快照、发送人名称快照、内容和发送时间。
- 留言成功后更新工单 `updated_at`，但不改变工单状态。
- 成功响应沿用 `MessageResponse` 白名单，不返回发送人底层 ID、邮箱、会话或认证敏感字段。

未扩大范围：

- 未实现 `PATCH /internal/tickets/{id}/status`。
- 未实现内部列表页和详情页动态接线。
- 未实现留言编辑、删除、附件、通知、SLA、统计、搜索或分页。

### 5.4 验证记录

专项测试：

```text
.\.venv\Scripts\python.exe -m pytest tests/api/test_internal_ticket_messages.py
```

结果：

```text
5 passed
```

M4 后端相关测试：

```text
.\.venv\Scripts\python.exe -m pytest tests/api/test_internal_ticket_reads.py tests/api/test_internal_ticket_assignment.py tests/api/test_internal_ticket_messages.py
```

结果：

```text
15 passed
```

全量回归：

```text
.\.venv\Scripts\python.exe -m pytest
```

结果：

```text
64 passed
```

说明：

- 本次未启动本地开发或测试服务，未占用端口。
- 已复查 `8000-8010` 常用测试端口，结果为无监听端口。
- 未使用子智能体，无需关闭子智能体线程。

## 6. M4-03 阶段结论

`M4-03` 内部公开留言权限已完成。

已满足：

- 负责客服可给本人负责且未关闭工单追加公开留言。
- 管理员可给任一未关闭工单追加公开留言，包括未分配工单。
- 非负责人客服不能给非本人负责工单留言。
- 客户不能调用内部留言接口。
- 未登录访问内部留言接口返回 `401`。
- 工单不存在返回 `404 RESOURCE_NOT_FOUND`。
- 已关闭工单新增留言返回 `409 CONFLICT`。
- 留言成功后更新工单 `updated_at`，但不改变工单状态。
- 留言响应字段不暴露发送人底层 ID、邮箱、负责人、审计记录或认证敏感字段。
- 非法请求体不会创建留言或更新工单。

未扩大范围：

- 状态推进和关闭限制仍属于 `M4-04`。
- 内部页面动态接线仍属于 `M4-05`。

下一步可进入 `M4-04`：线性状态推进和关闭后写入限制。

## 7. M4-04 启动记录

### 7.1 用户请求

用户要求进入 `M4-04`。

执行口径：

- 严格限定在 `M4-04`：线性状态推进和关闭后写入限制。
- 不提前实现内部列表页和详情页动态接线。
- 不扩大搜索、分页、统计、通知、附件、SLA 等范围外能力。

### 7.2 M4-04 范围

| 范围内 | 范围外 |
| --- | --- |
| `PATCH /api/v1/internal/tickets/{ticket_id}/status` | 内部页面动态接线 |
| 管理员可推进任一未关闭工单 | 状态批量更新 |
| 当前负责客服可推进本人负责的未关闭工单 | 客户确认关闭 |
| 状态只能线性推进到下一状态 | 工单重开 |
| 状态变化写入 `ticket_status_changed` 操作记录 | 关闭确认弹窗前端实现 |
| 状态变化不要求同时提交留言 | 搜索、分页、高级筛选、统计、附件、通知、SLA |
| 已关闭工单禁止继续状态操作 | 工单取消、删除 |
| 关闭后继续禁止分配和内部留言 | 外部通知 |

### 7.3 实现记录

本次未启动子智能体，由主智能体完成窄切片。

新增文件：

- `backend/tests/api/test_internal_ticket_status.py`

修改文件：

- `backend/app/api/routes/internal.py`
- `backend/app/repositories/json_repository.py`
- `backend/app/schemas/tickets.py`
- `backend/app/services/internal_ticket_service.py`

实现内容：

- 新增 `InternalTicketStatusUpdateRequest`，请求体仅允许 `status`，并使用工单状态枚举校验。
- 接通 `PATCH /api/v1/internal/tickets/{ticket_id}/status`。
- 仓储层在同一 JSON 事务内校验工单、关闭状态、客服负责人权限和线性状态转换。
- 支持的状态转换仅为 `unassigned -> processing -> resolved -> closed`。
- 管理员可推进任一未关闭工单，包括未分配工单。
- 客服仅可推进当前分配给自己的未关闭工单。
- 状态变化更新工单 `status` 与 `updated_at`。
- 状态变化写入 `ticket_status_changed` 操作记录，记录原状态、新状态、操作者和时间。
- 成功响应仅返回 `ticket_id`、当前状态和更新时间。
- 关闭后继续调用状态、分配、内部留言接口均返回冲突。

未扩大范围：

- 未实现内部列表页和详情页动态接线。
- 未实现客户确认关闭、工单重开、批量状态更新或通知。
- 未实现搜索、分页、高级筛选、统计、附件或 SLA。

### 7.4 验证记录

专项测试：

```text
.\.venv\Scripts\python.exe -m pytest tests/api/test_internal_ticket_status.py
```

结果：

```text
6 passed
```

M4 后端相关测试：

```text
.\.venv\Scripts\python.exe -m pytest tests/api/test_internal_ticket_reads.py tests/api/test_internal_ticket_assignment.py tests/api/test_internal_ticket_messages.py tests/api/test_internal_ticket_status.py
```

结果：

```text
21 passed
```

全量回归：

```text
.\.venv\Scripts\python.exe -m pytest
```

结果：

```text
70 passed
```

说明：

- 本次未启动本地开发或测试服务，未占用端口。
- 已复查 `8000-8010` 常用测试端口，结果为无监听端口。
- 未使用子智能体，无需关闭子智能体线程。

## 8. M4-04 阶段结论

`M4-04` 线性状态推进和关闭后写入限制已完成。

已满足：

- 管理员可将未关闭工单按线性状态推进。
- 管理员可直接将未分配的待分配工单推进为处理中。
- 负责客服可将本人负责的工单按线性状态推进至关闭。
- 非负责人客服不能推进状态。
- 客户不能调用内部状态接口。
- 工单不存在返回 `404 RESOURCE_NOT_FOUND`。
- 非法状态值返回 `422 VALIDATION_ERROR`。
- 跳级、回退、重复提交当前状态和关闭后继续推进均返回 `409 CONFLICT`。
- 状态变化写入 `ticket_status_changed` 操作记录。
- 状态变化不要求同时提交留言，也不自动创建留言。
- 工单关闭后继续分配、内部留言和状态推进均被拒绝。

未扩大范围：

- 内部页面动态接线仍属于 `M4-05`。
- 客户确认关闭、工单重开、批量状态更新、通知、附件、SLA、统计仍不属于 MVP 当前范围。

下一步可进入 `M4-05`：内部列表和工单详情页面及角色操作控件。

## 9. M4-05 启动记录

### 9.1 用户请求

用户要求进入 `M4-05`。

执行口径：

- 严格限定在 `M4-05`：内部列表和工单详情页面及角色操作控件。
- 接入已完成的 `M4-01` 至 `M4-04` 后端接口。
- 不新增后端业务能力。
- 不扩大搜索、分页、统计、通知、附件、SLA 等范围外能力。

### 9.2 M4-05 范围

| 范围内 | 范围外 |
| --- | --- |
| 内部工单列表页动态加载全部工单 | 多条件筛选、关键词搜索 |
| 内部列表按单一状态筛选 | 分页、批量操作、导出 |
| 内部详情页展示概要、描述、公开留言和操作记录 | 新增后端接口 |
| 管理员分配/重新分配控件 | 附件、通知、SLA、统计 |
| 管理员和负责客服状态推进控件 | 客户确认关闭、工单重开 |
| 管理员和负责客服留言控件 | 留言编辑、删除 |
| 非负责客服只读表现 | 独立视觉重设计 |
| 已关闭工单只读提示 | 前端以外的业务规则变更 |

### 9.3 实现记录

本次未启动子智能体，由主智能体完成页面联调。

新增文件：

- `frontend/assets/js/internal-tickets.js`

修改文件：

- `frontend/pages/internal/tickets.html`
- `frontend/pages/internal/ticket-detail.html`
- `frontend/assets/css/styles.css`

实现内容：

- 内部工单列表页调用 `GET /api/v1/internal/tickets`。
- 列表状态筛选通过 `status` 查询参数调用后端单状态筛选。
- 列表展示标题、客户、分类、中文状态、负责人、创建时间和详情入口。
- 内部详情页调用 `GET /api/v1/internal/tickets/{ticket_id}`。
- 详情页展示概要、问题描述、公开留言和操作记录。
- 公开留言按发送时间正序展示。
- 操作记录按后端返回的发生时间倒序展示。
- 管理员且工单未关闭时显示分配区，加载 `GET /api/v1/admin/agents` 作为负责人选项。
- 管理员和当前负责客服在工单未关闭时显示留言入口和唯一下一状态操作。
- 状态推进中的关闭操作增加确认提示。
- 非负责客服可查看详情、留言和记录，但不显示分配、留言和状态操作入口。
- 已关闭工单隐藏分配、留言和状态操作入口，显示只读提示。
- 标题、描述、留言、客户名、负责人名和操作记录均通过 `textContent` 或 DOM API 渲染。

未扩大范围：

- 未实现搜索、分页、多条件筛选、批量操作或导出。
- 未实现附件、通知、SLA、统计、客户确认关闭或工单重开。
- 未新增后端业务接口。

### 9.4 验证记录

前端脚本语法检查：

```text
node --check frontend\assets\js\internal-tickets.js
```

结果：通过。

全量后端回归：

```text
.\.venv\Scripts\python.exe -m pytest
```

结果：

```text
70 passed
```

浏览器联调使用临时端口 `8007` 和独立临时数据文件完成。

已验证：

| 场景 | 结果 |
| --- | --- |
| 管理员登录后进入内部工单列表 | 通过 |
| 内部列表展示 HTML/脚本文本为普通文本 | 通过 |
| 单一状态筛选 `待分配` | 通过 |
| 管理员打开内部详情 | 通过 |
| 管理员可见分配、留言和状态操作 | 通过 |
| 管理员分配工单给客服 | 通过 |
| 分配后负责人更新且操作记录可见 | 通过 |
| 管理员发送公开留言 | 通过 |
| 管理员将工单推进为处理中 | 通过 |
| 负责客服登录后可留言并推进状态 | 通过 |
| 非负责客服可查看但不能留言或推进状态 | 通过 |
| 已关闭工单隐藏处理控件并显示只读提示 | 通过 |
| 标题、描述和留言中的 HTML 片段不生成子节点或脚本节点 | 通过 |

清理：

- 浏览器联调结束后已关闭测试标签页。
- 临时服务端口 `8007` 已停止。
- 临时数据文件和服务日志已删除。
- 已复查 `8000-8010` 常用测试端口，结果为无监听端口。
- 未使用子智能体，无需关闭子智能体线程。

## 10. M4-05 阶段结论

`M4-05` 内部列表和工单详情页面及角色操作控件已完成。

已满足：

- 内部工单列表页可查看全部工单。
- 内部工单列表页可按单一状态筛选。
- 内部工单详情页可查看概要、描述、公开留言和操作记录。
- 管理员可在页面分配/重新分配、发送留言和推进状态。
- 负责客服可在页面发送留言和推进状态。
- 非负责客服可查看全部工单和详情，但不能处理非本人负责工单。
- 已关闭工单在页面上只读，不能继续分配、留言或推进状态。
- 页面展示用户可控文本时按普通文本渲染。
- 前端脚本语法检查、后端全量回归和浏览器主路径验证均通过。

未扩大范围：

- 未实现搜索、分页、多条件筛选、批量操作或导出。
- 未实现附件、通知、SLA、统计、客户确认关闭或工单重开。

## 11. M4 总结论

`M4` 内部处理闭环已完成。

已完成任务：

| 任务 | 结论 |
| --- | --- |
| `M4-01` 内部工单列表、详情和单状态筛选 API | 已完成 |
| `M4-02` 管理员分配与重新分配服务 | 已完成 |
| `M4-03` 内部公开留言权限 | 已完成 |
| `M4-04` 线性状态推进和关闭后写入限制 | 已完成 |
| `M4-05` 内部列表和详情页面联调 | 已完成 |

`M4` 达成实施计划完成标准：

- 管理员和客服可查看全部工单。
- 内部工单支持单一状态筛选。
- 管理员可分配和重新分配工单。
- 管理员和负责客服可发送公开留言。
- 管理员和负责客服可按线性状态推进至关闭。
- 非负责客服可查看但不能处理非本人负责工单。
- 分配、重新分配和状态变化记录可查。
- 关闭后写入限制生效。
- 内部页面可完成从列表到详情再到处理闭环的主路径。

下一步可进入 `M5-01`：统一错误响应和前端错误/成功反馈处理。
